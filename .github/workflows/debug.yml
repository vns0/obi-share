name: Debug Secrets

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print secret for debugging
        run: echo "DEBUG: The message length is: ${#secrets.DEBUG_MESSAGE}"
